import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'parse_problem',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
